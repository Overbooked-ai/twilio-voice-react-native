// This file serves as the entry point for Expo users.
// It re-exports the main library components, which will now automatically
// use the Expo-compatible native module on Android thanks to the changes
// in src/common.ts.

export * from '../index';
